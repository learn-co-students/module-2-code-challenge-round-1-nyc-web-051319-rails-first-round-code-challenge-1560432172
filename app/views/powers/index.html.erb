<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%=link_to power.name, "powers/#{power.id}" %>
      <br>
      Description: <%= power.description %>
    </li>
    <br>
  <% end %>
</ul>
<%= button_to('All Heroines', "/heroines", method: "get") %>
